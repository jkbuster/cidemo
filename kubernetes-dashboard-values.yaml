# Value overrides for kubernetes-dashboard Helm chart
#
# To install: helm install -n dashboard -f kubernetes-dashboard-values.yaml stable/kubernetes-dashboard
#

ingress:
  enabled: true
  hosts:
    - kubernetes-dashboard.jkb.io
    - k8s-db.jkb.io
